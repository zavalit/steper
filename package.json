{
  "name": "steper-monorepo",
  "private": true,
  "workspaces": [
    "packages/steper",
    "packages/react",
    "demo"
  ],
  "scripts": {

    "build:demo": "npm run build -w demo",
    "start:demo": "npm run start -w demo"
  }
}
